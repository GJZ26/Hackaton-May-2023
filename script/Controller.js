export default class Controller{
    constructor(type, player){
        this.type = type;
        this.player = player
    }

    read(e){
        if(this.type = "key"){
            
        }

        if(this.type = "xbox"){

        }
    }
}